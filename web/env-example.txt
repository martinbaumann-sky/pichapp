# Copia este archivo como .env y configura las variables

# Base de datos (obligatorio)
# Usa una cadena PostgreSQL valida. Ejemplo local:
# postgresql://postgres:postgres@localhost:5432/pichapp?schema=public
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?sslmode=prefer"
# URL directa (sin pool) requerida para `prisma migrate deploy`
DIRECT_URL="postgresql://USER:PASSWORD@HOST:PORT/DB"

# Base URL publica
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Sesiones
AUTH_SECRET="cambia-esta-clave-en-produccion"

# Mapas
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""
NEXT_PUBLIC_MAPBOX_TOKEN=""

# Correos
RESEND_API_KEY=""
RESEND_FROM_EMAIL="PichangApp <no-reply@pichangapp.cl>"
EMAIL_VERIFICATION_TTL_MINUTES="15"

# Pagos
MP_CLIENT_ID=""
MP_CLIENT_SECRET=""
MP_REDIRECT_URI="https://tu-app.com/api/mp/oauth/callback"
MP_WEBHOOK_URL="https://tu-app.com/api/mp/webhook"
MP_WEBHOOK_SIGNATURE_SECRET=""
MP_FEE_BPS="300"
MP_USE_SANDBOX="true"
MP_QR_USER_ID=""
MP_QR_POS_ID=""
KHIPU_RECEIVER_ID=""
KHIPU_SECRET_KEY=""
FLOW_API_KEY=""
FLOW_SECRET_KEY=""
FLOW_ENV="SANDBOX"
FINTOC_SECRET_KEY=""
TRANSBANK_COMMERCE_CODE=""
TRANSBANK_API_KEY=""
CRON_T60_SECRET=""
CRON_T60_GRACE_MINUTES="60"
ENCRYPTION_KEY="cambia-esta-clave-32-bytes"

# Rate limiting (Upstash)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""

# Configuracion admin inicial
# Actualiza estos valores con las credenciales reales en tu entorno local o de despliegue.
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="cambia-esta-clave"
ADMIN_USER_ID="admin-user-id"
ADMIN_EMAILS="admin@example.com"
NEXT_PUBLIC_ADMIN_EMAIL="admin@example.com"

# Supabase (solo si usas scripts opcionales)
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_SERVICE_ROLE_KEY=""
